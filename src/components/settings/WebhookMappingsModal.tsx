import React from 'react';

const WebhookMappingsModal: React.FC = () => {
  return (
    <div>
      <h2>Webhook Mappings Modal</h2>
      {/* Webhook mappings modal UI here */}
    </div>
  );
};

export default WebhookMappingsModal; 